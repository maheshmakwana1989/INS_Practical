#include<stdio.h>
#include<conio.h>

void main()
{
    int a=10;
    char message[100];
    printf("Enter your message:");
    gets(message);
    printf("Enter the key:");
    scanf("%d",&key);

    getch();
}